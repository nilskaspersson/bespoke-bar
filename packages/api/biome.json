{
	"$schema": "https://biomejs.dev/schemas/2.5.12/schema.json",
	"extends": ["@bespoke/config/biome.base.json"],
	"root": false,
	"files": {
		"includes": [
			"src/**",
			"vitest.config.ts",
			"!src/contract/mobile-contract.json"
		]
	},
	"linter": {
		"rules": {
			"style": {
				"noRestrictedImports": {
					"level": "error",
					"options": {
						"patterns": [
							{
								"group": ["react-dom", "react-dom/**", "@lexical/**"],
								"message": "packages/api is the SERVER package (Next-coupled): tRPC router/context, services, reads, cache-event model, auth, rate-limiting, LLM/Vision. It may import next/*, react (server cache only), @clerk, @upstash, @google*, stripe, the DB driver — but never client DOM (react-dom) or the editor (@lexical). Those belong in apps/bar."
							}
						]
					}
				}
			}
		}
	}
}
