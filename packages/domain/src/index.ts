/**
 * @bespoke/domain — pure, React-Native-safe logic.
 *
 * Consumers import by subpath (`@bespoke/domain/units/convert`,
 * `@bespoke/domain/recipes/stitchRecipe`, …) via the wildcard export, so this
 * root barrel stays intentionally empty to keep tree-shaking honest.
 */
export {};
