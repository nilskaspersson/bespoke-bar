{
	"name": "bespoke-bar",
	"version": "0.1.0",
	"private": true,
	"packageManager": "pnpm@11.8.0",
	"engines": {
		"node": "24.x"
	},
	"scripts": {
		"preinstall": "node scripts/only-pnpm.mjs",
		"dev": "turbo run dev",
		"build": "turbo run build",
		"lint": "turbo run lint",
		"test": "turbo run test",
		"check-types": "turbo run check-types",
		"deps:check": "pnpm outdated -r",
		"deps:up": "pnpm up -i -L -r"
	},
	"devDependencies": {
		"turbo": "2.10.0"
	}
}
